schema_version = 1
generated_notice = "Generated from scripts/proof-recipes.toml; do not edit by hand."

[[recipe]]
id = "runcompiled-construction"
status = "active"
triggers = [
  "goal-head:Func.RunCompiled",
  "goal-head:Func.RunCompiledTo",
  "goal-head:Func.ExecTo",
  "goal-head:Func.ExecWitness",
]
preferred_path = "Use `func_run` and its registered opcode arms. Before walking a large concrete body, apply the term-size breaker: the pathological case is a walk whose intermediate term carries the whole concrete remaining program, memory or value, and its signature is a `maxRecDepth`/`maxHeartbeats` ceiling over a multi-`MSTORE` staging run. Factor such a walk into named sub-components, abstract each over the memory or value it threads — a carrier structure over a variable, as `ConstructorPatchInvariant` does in `Blanc/LidoCircuitBreakerDeploymentTrace.lean` — and instantiate the concrete facts through named top-level lemmas, so one layer over a variable stays small instead of composing multiplicatively. Build each certificate separately and abstract only those that cross the breaker; the abstraction is not free and a short bounded walk does not need it. For residual-cost attribution, opt in with the default-off `Blanc.Forward.discharge` trace and aggregate it with `scripts/read-discharge-trace.py` before changing a fallback."
boundary = "This constructs a compiled walk; it does not invert an existing run, replace an already completed continuation with a summary, synthesize a parallel path certificate such as `DirectPausePath`, or optimize route data before the proof begins. A three-module inventory found the gas class heterogeneous (1,180 tactic / 90 residual) and every expensive value class heterogeneous, so no shared discharge fold is licensed. `LidoCircuitBreakerAttainment` instead measured repeated underconstrained `SourceStep` constructors and its one-path typed-helper pilot reduced 10.321 s to 5.971 s; that result does not license other route retrofits. Wave 4 found that exact proof-copy deletion moved `PauseWorldRunKit` only -1.1%, while a body-from-kernel summary moved `UnregisterRegistration` 38.482 to 21.610 s but required a new recursion ceiling and was reverted. Moving the same helper into `RegistrySubstrate` made that row 35.288 s and the gate red, so the cross-module extension is also closed. Those Wave 4 verdicts close two specific routes — a body-from-kernel summary and cross-module helper placement — and do not close the sub-component abstraction route in the preferred path. That route was measured separately on the 2026-08-23 deployment walk: a three-op prefix went from a 14.5 GiB store wall to a 0.8 s build, and the constructor body from a 15.5-minute hard stop with 12.6 GiB swap to a 737 ms compile. Those are whole-artifact build times recorded in that goal working log, not `check-elab.sh` rows, and no controlled measurement isolates the separate `DeploymentProof` privacy facade effect. The 2026-08-25 retrofit then dispositioned every named backport site. `pause_stageArgs_runCompiled` gained a local memory carrier: its target profiler entry fell from 29.898 s below the 2 s reporting threshold and its sequential owner row moved 47.842 to 31.984 s. A local Replacement carrier shared the guard-prefix composition over the existing staging suffix of the two replacement-registration body walks: their target entries fell from 28.371 and 41.545 s below 2 s and their owner row moved 30.441 to 2.326 s. All six associated recursion/heartbeat scopes disappeared. Cure 2 also ported the same leafwise construction route to the fresh-nonzero and absent-zero registration bodies: their owner rows moved 17.331 to 4.077 s and 15.033 to 3.190 s, and their remaining raised recursion/heartbeat scopes were removed. `registerPauser_stageArgs_runCompiled` retained its original concrete proof: a four-method carrier improved isolated attribution from 15.638 to 10.531 s but regressed the authoritative three-owner closure from Registry/Pause/Replacement 23.655/47.842/30.441 s to 37.989/67.316/53.222 s, so it was reverted. This is a measured non-candidate, not evidence that the technique is invalid; judge the owning closure, not a target-only profile. The 2026-08-21 `already_shared_cps` and `timing_refuted` screening rows answered whether these families could share a common helper, not whether the walked prefix carried an oversized concrete term. `ConstructorPatchInvariant`, `PauseStageMemory`, the Replacement guard-prefix certificates, `freshRegisterPauserBody_fromStage_runCompiled`, and `absentZeroRegisterPauserBody_fromStage_runCompiled` are the current local carrier/leaf-composition examples. `UnregisterRegistration` already contains an earlier hand-built equivalent, and an audit found no compelling WETH10 target. Cure 3 then executed that permitted local route on `pause_body_runCompiled`: eight private guard-prefix helpers reduced the fresh sequential owner row from 39.336 s to 11.819 s and its five-module owning closure from 79.790 s to 53.192 s, with no comparable failing `assumption` inside the factored helpers. The heartbeat scope was deleted after two clean default-heartbeat elaborations, and deterministic recursion-depth bisection right-sized the remaining command scope from 32768 to 1150 (1149 red, 1150 green). Cure 4's separate cold-entry case selected the narrow private `removeTarget_holeStorePrefix_runCompiled` boundary after four viable shapes; `PauseWorldRunKit` moved from 23.010 to 14.716 seconds. Reuse that narrow boundary only when the same accumulated cold-entry prefix recurs in a measured owner, before considering a broader continuation abstraction."
owner_module = "Blanc/Forward.lean"
canonical_example = "Blanc/Weth10Redeemable.lean:withdrawTo_progExecSat"
symbols = [
  "tactic:func_run",
  "declaration:Func.RunCompiled",
  "declaration:Func.RunCompiledTo",
  "declaration:Func.ExecTo",
  "declaration:Func.ExecWitness",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-25"

[[recipe]]
id = "linear-dispatch-selection"
status = "active"
triggers = ["goal-shape:linear-dispatch-selection"]
preferred_path = "For an existing `Func.RunCompiledTo` walk rooted at `Blanc.linearDispatchWith`, use `dispatchBodyWitness_of_runCompiledTo` for a selector hit or `dispatchFallbackWitness_of_runCompiledTo` for a miss. A hit supplies selector uniqueness and selected-entry membership; a miss supplies a nonempty entry list and exclusion from every entry. Both start from `selector :: tail`, remove the selector, recover the exact body or fallback call, and preserve the dispatch frame outside stack and gas. To construct an arbitrary-outcome all-miss route forward from a fallback-body witness, use `Func.execWitness_linearDispatchWith_fallback` with the exact `linearDispatchFallbackCost` budget. Compose frame facts with `Devm.DispatchFramePreserved.trans` and the push/pop/diff-burn adapters."
boundary = "The neutral theorems discharge only dispatcher opcode inversions or constructive all-miss opcode execution. They do not know a contract's selector census, calldata ABI, role storage, auxiliary rebasing, or selected-body/fallback semantics."
owner_module = "Blanc/LinearDispatchCorrectness.lean"
canonical_example = "Blanc/LinearDispatchCorrectness.lean:dispatchBodyWitness_of_runCompiledTo"
symbols = [
  "module:Blanc/LinearDispatch.lean",
  "module:Blanc/LinearDispatchCorrectness.lean",
  "declaration:Blanc.linearDispatchWith",
  "declaration:Blanc.selectorUnique",
  "declaration:Blanc.Devm.DispatchFramePreserved",
  "declaration:Blanc.Devm.DispatchFramePreserved.trans",
  "declaration:Blanc.dispatchFrame_of_pushBurn",
  "declaration:Blanc.dispatchFrame_of_popBurnBy",
  "declaration:Blanc.dispatchFrame_of_diffBurn",
  "declaration:Blanc.DispatchBodyWitness",
  "declaration:Blanc.dispatchBodyWitness_of_runCompiledTo",
  "declaration:Blanc.DispatchFallbackWitness",
  "declaration:Blanc.dispatchFallbackWitness_of_runCompiledTo",
  "declaration:Blanc.linearDispatchFallbackCost",
  "declaration:Blanc.Func.execWitness_linearDispatchWith_fallback",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-29"

[[recipe]]
id = "line-run-split"
status = "active"
triggers = ["implication-premise:Line.Run"]
preferred_path = "Use `line_execute` or `line_execute_with`; revert a named run premise first when needed."
boundary = "The tactic performs one split and does not automatically transport an arbitrary set of observations."
owner_module = "Blanc/CommonProofs.lean"
canonical_example = "Blanc/Conserved.lean:Fmint.of_prepApprove"
symbols = ["tactic:line_execute", "tactic:line_execute_with", "declaration:Line.Run"]
review_owner = "proof-infrastructure"
review_date = "2026-08-20"

[[recipe]]
id = "func-run-prefix-split"
status = "active"
triggers = ["implication-premise:Func.Run"]
preferred_path = "Use `func_execute n` or `func_execute_with line` to expose a known prefix. For a successful conditional whose selected arm calls a known nonreturning auxiliary, use `of_run_branch_call_of_not_run`; the `revert`, `revertWith`, and `revertReturnData` specializations discharge the standard reverter bodies."
boundary = "This targets the older implication-shaped `Func.Run`; it is not `RunCompiled` construction and does not invert an arbitrary named derivation without first reverting it. The nonreturning-call inversion proves only that the successful walk selected the zero/fall-through arm; transport any stack or state observation separately from its returned `Devm.PopBurn`."
owner_module = "Blanc/Tactics.lean"
canonical_example = "Blanc/Solvent.lean:withdraw_preserves_solvent"
symbols = [
  "tactic:func_execute",
  "tactic:func_execute_with",
  "declaration:Func.Run",
  "declaration:Blanc.of_run_branch_call_of_not_run",
  "declaration:Blanc.of_run_branch_call_revert",
  "declaration:Blanc.of_run_branch_call_revertWith",
  "declaration:Blanc.of_run_branch_call_revertReturnData",
  "declaration:Blanc.Func.not_run_revertReturnData",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-20"

[[recipe]]
id = "stack-prefix-transport"
status = "active"
triggers = ["goal-shape:stack-prefix-line-run"]
preferred_path = "Use `line_prefix` or `generalize_line_prefix`, with `show_pref` for concrete prefix goals. For a known MUL, DIV, TIMESTAMP, XOR, or non-address argument-check step, use the corresponding `prefix_of_*` declaration directly when the tactic has no registered arm."
boundary = "`line_prefix` supports a finite instruction set and refuses instructions without a registered case. The direct `prefix_of_*` lemmas transport only the named stack prefix; combine them with a separate observation invariant when more state must be carried."
owner_module = "Blanc/Tactics.lean"
canonical_example = "Blanc/Weth10HolderFlowCompiled.lean:recognized_of_run_dispatchWith"
symbols = [
  "tactic:line_prefix",
  "tactic:generalize_line_prefix",
  "tactic:show_pref",
  "declaration:prefix_of_mul",
  "declaration:prefix_of_div",
  "declaration:prefix_of_timestamp",
  "declaration:prefix_of_xor",
  "declaration:prefix_of_argCheckNonAddress",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-20"

[[recipe]]
id = "state-context-cleanup"
status = "active"
triggers = ["context-shape:intermediate-devm"]
preferred_path = "Use `clear_state hState` after transporting every fact that must survive."
boundary = "This is destructive context cleanup: it removes the state and all local facts that depend on it. The continuation-summary pilot found this trigger to be a false positive after a continuation was already complete; direct reuse of the existing generic summary added two source lines and no measurable elaboration win. Wave 4 supplied a second eligible false positive in the exact-helper reuse experiment: the goal was discharged by an existing `RunCompiled` summary, so clearing state would only destroy usable hypotheses. The trigger deliberately remains the bare more-than-two-`Devm` heuristic because summary availability is semantic, not recoverable from the context count; narrowing by goal head would introduce false negatives in real construction goals. Treat the trigger as a prompt to check for a completed summary first, never as an automatic cleanup command."
owner_module = "Blanc/Tactics.lean"
canonical_example = "Blanc/Conserved.lean:Fmint.of_prepApprove"
symbols = ["tactic:clear_state"]
review_owner = "proof-infrastructure"
review_date = "2026-08-21"

[[recipe]]
id = "line-observation-invariance"
status = "active"
triggers = ["goal-head:Line.Inv", "goal-head:Ninst.Inv", "goal-head:Rinst.Inv"]
preferred_path = "Use `line_inv` through the registered `Ninst.Hinv` and `Rinst.Hinv` instances."
boundary = "A missing contract-neutral instance belongs in the lowest common upstream layer; contract-specific semantic facts do not."
owner_module = "Blanc/Tactics.lean"
canonical_example = "Blanc/Weth10HolderFlowCompiled.lean:Devm.DispatchSilent.of_pushEq"
symbols = [
  "tactic:line_inv",
  "declaration:Line.Inv",
  "declaration:Ninst.Inv",
  "declaration:Rinst.Inv",
  "declaration:Ninst.Hinv",
  "declaration:Rinst.Hinv",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-20"

[[recipe]]
id = "function-observation-invariance"
status = "active"
triggers = ["goal-head:Func.Inv", "goal-head:Linst.Inv"]
preferred_path = "For `Func.Inv`, use `func_inv` to assemble the function invariant from registered line and terminal invariants. For a terminal `Linst.Inv`, use the registered instance directly with `exact Linst.Hinv.inv`."
boundary = "`func_inv` deliberately refuses `Func.call`, whose callee is arbitrary under `Func.Inv`; fix the context or factor through the entry. A missing terminal instance belongs in the lowest common shared module, not in a contract consumer."
owner_module = "Blanc/Tactics.lean"
canonical_example = "Blanc/Solvent.lean:approve_preserves_bal"
symbols = [
  "tactic:func_inv",
  "declaration:Func.Inv",
  "declaration:Linst.Inv",
  "declaration:Linst.Hinv",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-20"

[[recipe]]
id = "call-boundary-outcomes"
status = "active"
triggers = ["goal-head:Func.ExecSat", "goal-head:Prog.ExecSat", "goal-head:Func.ExecWitness"]
preferred_path = "Use the `ForwardCall` module and the live `ExecSat`/`ExecWitness` layer to cross calls or package multiple outcomes. At a proof-carrying `DELEGATECALL` boundary, use `DelegatecallSpawnDescriptor.step` for the exact spawn and `.crossing` for the entered child's successful continuation instead of reconstructing the descriptor fields at the consumer. To construct the compiled call from a retained child and exact resume equation, use `DelegatecallSpawnDescriptor.runCompiled_of_certificate`. When an existing compiled call step has already resumed, use `DelegatecallSpawnDescriptor.settled_of_runCompiled` to obtain a `DelegatecallSettledBoundary` with the exact retained child and parent observations. Use `DelegatecallSettledBoundary.memory_image_of_outputSize_zero` to carry a proof-carrying memory image across a zero-output child resume."
boundary = "Do not duplicate the settlement/determinism tail, and do not infer deadness from qualified-name grep alone."
owner_module = "Blanc/ForwardCall.lean"
canonical_example = "Blanc/Weth10Redeemable.lean:withdrawTo_progExecSat"
symbols = [
  "module:Blanc/ForwardCall.lean",
  "module:Blanc/DelegatecallEnvelope.lean",
  "declaration:Func.ExecSat",
  "declaration:Prog.ExecSat",
  "declaration:Func.ExecWitness",
  "declaration:Prog.ExecWitness",
  "declaration:DelegatecallSpawnDescriptor.afterAccess_memory",
  "declaration:DelegatecallSpawnDescriptor.step",
  "declaration:DelegatecallSpawnDescriptor.child_data",
  "declaration:DelegatecallSpawnDescriptor.crossing",
  "declaration:DelegatecallSpawnDescriptor.runCompiled_of_certificate",
  "declaration:DelegatecallSettledBoundary",
  "declaration:DelegatecallSpawnDescriptor.settled_of_runCompiled",
  "declaration:DelegatecallSettledBoundary.memory",
  "declaration:DelegatecallSettledBoundary.memory_eq_parent_of_outputSize_zero",
  "declaration:DelegatecallSettledBoundary.memory_image_of_outputSize_zero",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "devm-projection-bridge"
status = "active"
triggers = ["goal-shape:devm-update-projection"]
preferred_path = "Rewrite with the matching Jaune update-first projection lemma, named `Devm.<update>_<projection>`, for the column in the goal. Never bridge a concrete effect tower or compiled artifact through `withOutput`, `setMach`, `setMeta`, `setWorld`, or another `with*` update using bare `change`, `show`, `rfl`, or `exact`."
boundary = "A succeeding concrete `getStor` walk can expose the same projection mechanism after the effect tower has already been built. Cure 4 routed that case through the shared `Devm.withRefundCounter_getStor` and `Devm.addLog_getStor` semantic cuts plus a private cold-store boundary, moving `LidoCircuitBreakerDeploymentTrace` from 62.173 to 17.050 seconds. Reuse the earliest matching semantic projection cut; do not reorder `Meta` fields. This does not reopen `successor-projection-normalization`: that measured refutation attacked `setMach` chains in an owner dominated by unrelated kernel checks. Resource ceilings neither detect nor bound this kernel-side class. The matcher needs an explicit `Devm` update head in the target; when a local definition hides the chain, unfold only that binding and invoke `blanc_suggest` again."
owner_module = "Blanc/LidoCircuitBreakerDeploymentMessage.lean"
canonical_example = "Blanc/LidoCircuitBreakerDeploymentMessage.lean:officialConstructorPost_refundCounter"
symbols = ["declaration:LidoCircuitBreaker.officialConstructorPost_refundCounter"]
review_owner = "proof-infrastructure"
review_date = "2026-08-25"

[[recipe]]
id = "bytesize-composition"
status = "active"
triggers = ["goal-shape:compileshape-bytesize"]
preferred_path = "Prove one small `decide +kernel` fact per leaf, then derive internal `compileShape.byteSize` facts arithmetically through `dispatchNode_size`-style composition. `dispatchCae9_size` is the canonical example: with its children available, its composition closes in 0.004 s."
boundary = "The measured law is approximately 2.6 ms per compiled byte of the addressed object; byte-range width predicts nothing because `byteAtByShape` is lazy. Cure 2 applied the existing composition route in `Weth10Deploy`: named size facts replaced repeated closed decisions and moved its owner row from 43.283 to 29.463 s. The narrower `Weth10DeployDomainSlices` packet first had to add its missing child facts and then regressed from 16.628 to 17.734 s, so it was reverted; reopen that family only with a broader child-fact or representation change whose owner row wins. `weth10MainEmit_drop_3950` costs approximately 0.011 s and should remain unchanged."
owner_module = "Blanc/Weth10Deploy.lean"
canonical_example = "Blanc/Weth10Deploy.lean:dispatchCae9_size"
symbols = ["declaration:Weth10.dispatchCae9_size"]
review_owner = "proof-infrastructure"
review_date = "2026-08-25"

[[recipe]]
id = "successor-projection-normalization"
status = "partial"
triggers = ["goal-shape:successor-projection"]
preferred_path = "Use an existing named, oriented, one-layer projection lemma when one already serves the goal; otherwise keep the explicit local normalization."
boundary = "Do not replace deep state towers with transparent abbreviations or broad unfolding; `RegistrySubstrate` records why that diverges. On the heartbeatAfterCount/Expiry/Interval tower, adding six one-layer projection lemmas and six retrofits regressed module elaboration from 40.075 s to 41.944 s. Wave 3 then found `LidoCircuitBreakerAccess` dominated by 27–28 s kernel checks, not its secondary setMach chains. Wave 4 attributed the Registry tail to `directPauseControl_gas` (defeq/whnf plus kernel checking) and `directPauseControl_run` (kernel checking); all later declarations stayed below 2 s and none of the module's 212 `Devm.setMach_setMach` citations occurs after that obstacle. This does not reopen S3, S1, S2, or a module split."
owner_module = "Blanc/Forward.lean"
canonical_example = "Blanc/Forward.lean:Devm.getStorVal_setMach"
symbols = ["declaration:Devm.getStorVal_setMach"]
review_owner = "proof-infrastructure"
review_date = "2026-08-21"

[[recipe]]
id = "runcompiled-family-compression"
status = "partial"
triggers = ["goal-shape:runcompiled-family-compression"]
preferred_path = "When expensive bodies repeat the same post-kernel walk, freeze a committed-row decision rule, factor the body-from-kernel boundary, and preserve the old statements as instantiations. If a one-shot `func_run` needs a local resource ceiling, promote its exact tactic-produced residual states to named theorem boundaries, reducing the chunks as far as needed; compare against the original declarations' limits before rejecting the factorization. Profile the generic, chunks, and instances, run the bare elaboration gate, withhold the generic in an isolated falsifier, and reject any split that materially regresses its owner row or adds a proof-resource ceiling."
boundary = "G1: deleting 687 exact proof-copy lines moved `PauseWorldRunKit` 23.536 to 23.273 s (-1.1%) and raised `RegistrySubstrate` 17.513 to 26.909 s, so exact source duplication alone is not a timing signal. G2 correction: the earlier rejection was not like-for-like because the original registration bodies already carried `maxRecDepth 16384` and `maxHeartbeats 2400000`. The 24-step generic and a 4+20 same-declaration split failed at default limits, but named two-instruction mask chunks plus named argument, branch, admin, and static-prefix summaries succeeded. Reusing one private body-from-kernel theorem across all four zero-pauser bodies removed eight resource scopes and moved `UnregisterRegistration` 38.482 to 5.318 s (-86.2%); the two-old-last intermediate measured 21.359 s. G3 remains refuted: shared placement for `ReplacementRegistration.oldLastNonzero` made `RegistrySubstrate` 35.288 s (>2x baseline), so cross-module reuse still needs independent ancestor headroom. G4: the Registry tail is sparse and kernel/defeq-owned, not a compression family. G5: `PauseWorldRun` (8.995 s), the WETH accounting pair (25.573 s combined), and the rich/local twin (4.424 s) are maintainability-only; source symmetry does not license a timing retrofit."
owner_module = "Blanc/LidoCircuitBreakerUnregisterRegistration.lean"
canonical_example = "Blanc/LidoCircuitBreakerUnregisterRegistration.lean:registerPauser_body_foundZeroOldLast_runCompiled"
symbols = [
  "declaration:Blanc.LidoCircuitBreaker.registerPauser_stageArgs_runCompiled",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-21"

[[recipe]]
id = "shared-subject-kernel-decision"
status = "active"
triggers = ["goal-shape:shared-subject-kernel-decision"]
preferred_path = "When several kernel-decidable facts inspect the same expensive closed subject, bind that subject once, decide the facts as one tuple or conjunction, and project the results; alternatively prove one normalized equality and derive the views with `congrArg`."
boundary = "This applies only when normalization of one identical closed subject dominates every fact. It does not license bundling facts about different subjects, and it is not a term-size or definitional-equality cure. In cure 2, six Attainment pins bundled over `runtimePersistentSourceSites officialParams` cost the sum rather than the max and regressed the owner row from 32.134 to 53.652 s; for that family, free-variable aliases and conjunction elaboration defeated the rule. Reopen Attainment only with a shared closed-subject representation that avoids those costs."
owner_module = "Blanc/LidoCircuitBreakerSites.lean"
canonical_example = "Blanc/LidoCircuitBreakerSites.lean:runtimeSourceEffectPcs_official"
symbols = ["declaration:Blanc.LidoCircuitBreaker.runtimeSourceEffectPcs_official"]
review_owner = "proof-infrastructure"
review_date = "2026-08-25"

[[recipe]]
id = "selector-separation"
status = "active"
triggers = ["goal-shape:selector-separation"]
preferred_path = "Hoist a reviewed literal separation table ahead of repeated consumers and transport its facts directly; the ExecAccounting pilot reduced its module profile from 46.28 s to 17.74 s across 16 retrofits."
boundary = "Cure 2 completed the module-local retrofit: the remaining 28 name-selector kernel decisions now rewrite through the existing numeral bridges, and the approve straggler uses its hoisted separation certificate. Together those changes moved `Weth10HolderFlowExecAccounting` from 17.056 to 13.067 s at the final checkpoint. This does not establish I3's cross-domain canonical selector list, `Nodup` theorem, extractor, named simp set, or cross-module placement; those are separate designs and no blocking rule may require them. The earlier whitespace-only compaction remains a source-size result with no timing claim. `blanc_suggest` still misses the literal `SelectorWordNoPrimaryFlow` certificate goal, so callers must consult this recipe manually for that shape."
owner_module = "Blanc/Weth10SelectorFacts.lean"
canonical_example = "Blanc/Weth10SelectorFacts.lean:Weth10.selector_name_ne_approveSelector"
symbols = ["declaration:Weth10.selector_name_ne_approveSelector"]
anti_patterns = ["local-selector-table"]
review_owner = "proof-infrastructure"
review_date = "2026-08-21"

[[recipe]]
id = "fixed-byte-offsets"
status = "active"
triggers = ["goal-shape:fixed-byte-offset"]
preferred_path = "Use `Bytes.sliceD_writeAt` for the whole written window, `Bytes.sliceD_writeAt_inside` for a contained subwindow, and `Bytes.sliceD_writeAt_before` or `Bytes.sliceD_writeAt_after` for disjoint neighboring windows. For a padded slice, use `Bytes.getD_sliceD_of_lt` for an in-range byte or `Bytes.sliceD_sliceD_of_le` for a contained subwindow. Use `Bytes.readWord_writeAt_self` or `Bytes.readWord_writeAt_of_disjoint` for word reads. For adjacent event words use `Bytes.read_two_word_writes_at` or `Mem.read_two_word_writes_at`. For padded or abstract memory, start from `Mem.Wf` and `Mem.Reads`; in scratch decoders, `of_run_mstoreAt_image` and `of_run_loadWordAt_image` advance the stack, image, well-formedness, and state equation together, while `of_run_loadWordAt_logs` preserves event chronology. For exact primitive byte-store inversion use `of_run_mstore8_val` or its known-prefix wrapper `prefix_of_mstore8_val`, and use `of_run_mstore8_state` for the persistent-state equation. For calldata copies with known operands use `prefix_of_calldatacopy_val`. For creation copies use `of_run_codecopy_image`; its lower-level components are `prefix_of_codecopy_val` plus `of_run_codecopy_logs`. For fixed logs use `of_logWith_image`. Keep compiled-emitter `List.drop` equalities local unless a profile proves that a structural helper moves their kernel cost."
boundary = "These laws cover byte-array writes and fixed-width word reads. They do not turn arbitrary compiled-emitter `List.drop` identities into a shared API. `LidoCircuitBreakerEnumeration` was dominated by 34–35 s kernel checks unrelated to byte offsets. On `Weth10Deploy`, a two-next/branch helper changed its 41.5–41.7 s proof by only 0.6–1.1%, and a later top-level private tail theorem moved the owner median only from 48.11 to 46.85 s (-2.62%); both pilots were reverted. Reopen that separate emitter route only for a change whose serialized owner median improves by the licensed win rule."
owner_module = "Blanc/CommonProofs.lean"
canonical_example = "Blanc/CommonProofs.lean:Bytes.readWord_writeAt_of_disjoint"
symbols = [
  "module:Blanc/CommonProofs.lean",
  "declaration:Bytes.getD_sliceD_of_lt",
  "declaration:Bytes.sliceD_sliceD_of_le",
  "declaration:Bytes.sliceD_writeAt",
  "declaration:Bytes.sliceD_writeAt_inside",
  "declaration:Bytes.sliceD_writeAt_before",
  "declaration:Bytes.sliceD_writeAt_after",
  "declaration:Bytes.readWord_writeAt_self",
  "declaration:Bytes.readWord_writeAt_of_disjoint",
  "declaration:Bytes.read_two_word_writes_at",
  "declaration:Mem.Wf",
  "declaration:Mem.Reads",
  "declaration:Mem.read_two_word_writes_at",
  "declaration:of_run_mstoreAt_image",
  "declaration:of_run_mstore8_val",
  "declaration:prefix_of_mstore8_val",
  "declaration:of_run_mstore8_state",
  "declaration:of_run_loadWordAt_image",
  "declaration:of_run_loadWordAt_logs",
  "declaration:prefix_of_calldatacopy_val",
  "declaration:of_run_codecopy_image",
  "declaration:prefix_of_codecopy_val",
  "declaration:of_run_codecopy_logs",
  "declaration:of_logWith_image",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "frame-root-carrying-execution"
status = "active"
triggers = ["goal-shape:frame-root-carrying"]
preferred_path = "Use `rootedRunCompiledTo` to carry a predicate through a compiled walk, discharge childless instructions with `ninstAllChildRoots_of_not_exec` or `NonExecInstruction`, establish spawning children with `ninstAllChildRoots_of_exec_spawn`, and finish a whole program with `Prog.exec_of_rootedRunCompiledTo`."
boundary = "This API preserves predicates over raw entered-frame roots. It does not apply settlement/commit filtering; use `ExecutionSettlement` and `ExecutionOccurrence` for retained or committed histories."
owner_module = "Blanc/RootedExecution.lean"
canonical_example = "Blanc/RootedExecution.lean:Prog.exec_of_rootedRunCompiledTo"
symbols = [
  "module:Blanc/RootedExecution.lean",
  "declaration:rootedRunCompiledTo",
  "declaration:ninstAllChildRoots",
  "declaration:ninstAllChildRoots_of_not_exec",
  "declaration:ninstAllChildRoots_of_exec_spawn",
  "declaration:funcExecFree",
  "declaration:rootedRunCompiledTo_of_execFree",
  "declaration:Prog.exec_of_rootedRunCompiledTo",
  "declaration:NonExecInstruction",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-29"

[[recipe]]
id = "trace-admitted-frame-invariant"
status = "active"
triggers = ["goal-head:ContractSpec.PreservesAdmitted"]
preferred_path = "State the target program obligation as `ContractSpec.SoundAdmitted ca entry`, keeping the concrete `Exec.FrameAdmitted` premise result-free, then apply `ContractSpec.preserves_inv_admitted`. Use `Exec.FrameAdmitted.root` at the selected target entry and the named child/continuation restriction lemmas rather than rebuilding raw-frame list membership. When the program proof needs the ordinary message-entry empty stack and memory, conjoin its independent premise with `Exec.FreshEntry` using `Exec.FrameAdmitted.and`; retained wrappers derive the fresh half through their `freshFrameAdmitted` theorem. Drop to `preserves_lift_admitted` or `lift_inv_admitted` only when the standard `ContractSpec.PreWf` carrier is insufficient."
boundary = "Admission ranges over raw actually entered frame roots and is not a settlement filter or a postcondition. `freshFrameAdmitted` certifies only empty stack and memory from actual entered frames; it does not manufacture an environment, storage, delegation, or precompile fact. The caller must establish every independent entry premise for the concrete execution or retained trace; this recipe does not weaken `PreservesAdmitted` to an endpoint premise or replace the forward `RootedExecution` construction API."
owner_module = "Blanc/ContractAdmission.lean"
canonical_example = "Blanc/ContractAdmission.lean:ContractSpec.preserves_inv_admitted"
symbols = [
  "module:Blanc/ExecutionFrames.lean",
  "module:Blanc/ExecutionFrameEntry.lean",
  "module:Blanc/ExecutionAdmission.lean",
  "module:Blanc/ContractAdmission.lean",
  "declaration:Blanc.Exec.FrameAdmitted",
  "declaration:Blanc.Exec.FrameAdmitted.root",
  "declaration:Blanc.Exec.FreshEntry",
  "declaration:Blanc.Exec.FrameAdmitted.and",
  "declaration:Blanc.ForallSubExecAdmitted",
  "declaration:Blanc.lift_admitted",
  "declaration:Blanc.lift_inv_admitted",
  "declaration:Blanc.ContractSpec.SoundAdmitted",
  "declaration:Blanc.ContractSpec.PreservesAdmitted",
  "declaration:Blanc.ContractSpec.preserves_lift_admitted",
  "declaration:Blanc.ContractSpec.preserves_inv_admitted",
]
review_owner = "proof-infrastructure"
review_date = "2026-09-01"

[[recipe]]
id = "message-execution-settlement"
status = "active"
triggers = ["goal-shape:message-execution-settlement"]
preferred_path = "Use `MessageExecution.processMessage_eq_settle_exec_of_enter` when an exact successful `Frame.enter` equation is already retained, including for delegated children. For payable ordinary calls, derive a normal non-precompile entry with `executeCode_enter_of_codeAddress_not_precompile` and use the `*_afterTransfer_of_codeEntry` adapters; the `*_afterTransfer_of_notPrecompile` bridge packages the same route from address evidence. Use `processMessage_eq_settle_exec_afterTransfer_of_noCodeAddress` for creation-code entry, or `processMessage_eq_settle_exec_afterTransfer` when precompiles are explicitly disabled. Then use the clean/revert/halt adapters and canonical `settledRevert` or `settledHalt` machines instead of unfolding message settlement at the contract site. Use the unsuffixed adapters for the identity-entry specialization. For an already-retained `ProcessMessage`, use `processMessage_clean_rawPost` to recover the successful raw post, `processMessage_entry_facts` for the actual entry frame projections, and the separate `processMessage_entry_stack` / `processMessage_entry_memory` empty-entry projections. For an actual retained zero-value static-precompile child whose routing already selected the native precompile, use `stor_of_processMessage_staticPrecomp`; at address `0x2` on exactly 64 input bytes, use `gasSha25664_le_of_processMessage_clean`, `output_of_processMessage_sha256_64_clean`, or their combined `frame_of_processMessage_sha256_64_clean` image."
boundary = "The generic enter bridge requires only the exact successful entry equation. Post-transfer bridges name the successful transferred environment: exact-code-entry and non-precompile variants work with ordinary `disablePrecompiles = false` messages, the no-code-address variant covers creation code, and the convenience bridge requires disabled precompiles. The unsuffixed specialization additionally requires entry-state identity. Retained-frame inversion exposes storage equality rather than whole-state equality because value transfer may change balances. Static-precompile inversion does not prove native routing: its enabled-precompile and clean-child premises remain load-bearing, delegation must already be ruled out, and the SHA result is fixed only for address 2 and exactly 64 bytes. These facts describe ordinary call-message settlement, not CREATE settlement."
owner_module = "Blanc/MessageExecution.lean"
canonical_example = "Blanc/MessageExecution.lean:MessageExecution.processMessage_eq_settle_exec"
symbols = [
  "module:Blanc/MessageExecution.lean",
  "module:Blanc/MessageExecutionInversion.lean",
  "module:Blanc/StaticPrecompileMessage.lean",
  "declaration:MessageExecution.processMessage_eq_settle_exec_of_enter",
  "declaration:MessageExecution.frameEnter_eq_run_afterTransfer_of_notPrecompile",
  "declaration:MessageExecution.processMessage_eq_settle_exec_afterTransfer_of_notPrecompile",
  "declaration:MessageExecution.processMessage_eq_settle_exec_afterTransfer_of_noCodeAddress",
  "declaration:MessageExecution.processMessage_eq_settle_exec_afterTransfer",
  "declaration:MessageExecution.processMessage_eq_settle_exec",
  "declaration:MessageExecution.processMessage_clean_of_exec_afterTransfer",
  "declaration:MessageExecution.processMessage_eq_settle_exec_afterTransfer_of_codeEntry",
  "declaration:MessageExecution.processMessage_clean_of_exec_afterTransfer_of_codeEntry",
  "declaration:MessageExecution.executeCode_enter_of_codeAddress_not_precompile",
  "declaration:MessageExecution.processMessage_clean_of_exec",
  "declaration:MessageExecution.processMessage_revert_of_exec_afterTransfer",
  "declaration:MessageExecution.processMessage_revert_of_exec",
  "declaration:MessageExecution.processMessage_halt_of_exec_afterTransfer",
  "declaration:MessageExecution.processMessage_halt_of_exec",
  "declaration:MessageExecution.processMessage_clean_rawPost",
  "declaration:MessageExecution.processMessage_entry_facts",
  "declaration:MessageExecution.processMessage_entry_stack",
  "declaration:MessageExecution.processMessage_entry_memory",
  "declaration:Blanc.stor_of_processMessage_staticPrecomp",
  "declaration:Blanc.gasSha25664_le_of_processMessage_clean",
  "declaration:Blanc.output_of_processMessage_sha256_64_clean",
  "declaration:Blanc.frame_of_processMessage_sha256_64_clean",
  "declaration:MessageExecution.settledRevert",
  "declaration:MessageExecution.settledHalt",
  "declaration:Msg.initDevm_stack",
  "declaration:Msg.initSevm_data",
]
review_owner = "proof-infrastructure"
review_date = "2026-09-01"

[[recipe]]
id = "raw-sstore-free-compiled-path"
status = "active"
triggers = ["goal-shape:raw-sstore-free-compiled-path"]
preferred_path = "Build `Func.RunCompiledTo.NoRawSstorePath` over the exact selected compiled derivation, supplying childlessness for every reached external instruction. Use `NoRawSstorePath.of_entrySstoreFree_reachableExecFree` when the executable finite-component checkers prove both local SSTORE freedom and reachable exec freedom; otherwise use `NoRawSstorePath.of_execFree` for an execution-free, locally SSTORE-free body, `NoRawSstorePath.of_prepend_nonexec` for an instruction-only prefix, `NoRawSstorePath.of_revertWith` for a symbolic constant-error body, or `NoRawSstorePath.of_emptyRevertGuard` for a selected nonzero guard calling an empty-revert auxiliary. When a failing prefix is checker-safe only with a harmless success continuation, certify that source and use `NoRawSstorePath.replaceStopWith_of_error` to reinstate the production continuation that the exact error path never enters; `Func.replaceStopWith_prepend` normalizes an instruction-only prefix around that replacement. For a warm fixed-width SHA-256 precompile step, preserve the empty child slot with `Ninst.childlessRunCompiled_staticcall_sha256_64_warm_ext`; finish with `Prog.exists_exec_noRawSstore`. When an exact `Exec` already exists, use `Exec.noRawSstore_of_exactMain_entrySstoreFree_reachableExecFree` to combine the two executable entry certificates occurrence-first."
boundary = "This is raw construction-direction chronology, not rollback reasoning. An empty retained-write list or reverted terminal state does not prove the certificate because an earlier raw SSTORE may have executed and then rolled back. Entered child frames require their own evidence; synchronously resolved childless precompiles may use the explicit done-frame constructor."
owner_module = "Blanc/ForwardNoRawSstore.lean"
canonical_example = "Blanc/ForwardNoRawSstore.lean:Func.RunCompiledTo.NoRawSstorePath.of_execFree"
symbols = [
  "module:Blanc/ForwardNoRawSstore.lean",
  "declaration:Blanc.Ninst.ChildlessRunCompiled",
  "declaration:Blanc.Ninst.ChildlessRunCompiled.toRunCompiled",
  "declaration:Blanc.Ninst.childlessRunCompiled_exec_doneFrame",
  "declaration:Blanc.Ninst.childlessRunCompiled_staticcall_doneFrame",
  "declaration:Blanc.Ninst.childlessRunCompiled_staticcall_sha256_64_warm_ext",
  "declaration:Blanc.emptyRevertGuardCost",
  "declaration:Blanc.Func.runCompiledTo_emptyRevertGuard",
  "declaration:Blanc.Exec.NoRawSstore",
  "declaration:Blanc.Func.RunCompiledTo.NoRawSstorePath",
  "declaration:Blanc.Func.RunCompiledTo.NoRawSstorePath.of_execFree",
  "declaration:Blanc.Func.RunCompiledTo.NoRawSstorePath.of_revertWith",
  "declaration:Blanc.Func.RunCompiledTo.NoRawSstorePath.of_emptyRevertGuard",
  "declaration:Blanc.Func.RunCompiledTo.NoRawSstorePath.of_prepend_nonexec",
  "declaration:Blanc.Func.RunCompiledTo.NoRawSstorePath.of_entrySstoreFree_reachableExecFree",
  "declaration:Blanc.Func.replaceStopWith",
  "declaration:Blanc.Func.replaceStopWith_prepend",
  "declaration:Blanc.Func.RunCompiledTo.NoRawSstorePath.replaceStopWith_of_error",
  "declaration:Blanc.Prog.exists_exec_noRawSstore",
  "declaration:Blanc.Exec.noRawSstore_of_exactMain_entrySstoreFree_reachableExecFree",
  "declaration:Blanc.Exec.NoRawSstore.no_successfulSstoreOccurrence",
  "declaration:Blanc.Exec.NoRawSstore.retainedStorageWrites_eq_nil",
  "declaration:Blanc.Exec.NoRawSstore.retainedStorageEffectTriples_eq_nil",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "retained-write-noninterference"
status = "active"
triggers = ["goal-shape:retained-write-noninterference"]
preferred_path = "For `Exec.NoRetainedWriteTo`, split first on `Execution.commits out = true`. Close the rollback arm with `Exec.noRetainedWriteTo_of_not_commits`; on a committing exact-main invocation use `Exec.noRetainedWriteTo_of_exactMain_reachableExecFree` when the selected entry and its finite internal-call component pass `Prog.reachableExecFree`. For a dispatcher-selected non-main entry, route an actual `SourceCursor` through `Toward.linearDispatchWith_selectedBody`, discharge same-frame exec absence with `SourceCursor.noExec_of_reachableExecFree`, and finish with `Exec.noRetainedWriteTo_of_no_sameFrame_execAt`. Whole-program source-childlessness and entered-frame owner separation remain the other committing routes."
boundary = "The noncommitting theorem proves retained-write absence by rollback, not raw instruction absence. `Prog.reachableExecFree` checks both arms and a finite lookup-resolved call-closed source component, but says nothing about unselected entries, child outcomes, commitment, gas, or liveness. The exact-main endpoint applies only to `program.main`; a selected dispatcher body needs the explicit actual-route cursor bridge. Do not infer childlessness merely from a static call flag."
owner_module = "Blanc/ExecutionNoninterference.lean"
canonical_example = "Blanc/ExecutionNoninterference.lean:Exec.noRetainedWriteTo_of_not_commits"
symbols = [
  "module:Blanc/ExecutionNoninterference.lean",
  "declaration:Exec.NoRetainedWriteTo",
  "declaration:Exec.noRetainedWriteTo_of_not_commits",
  "declaration:Exec.noRetainedWriteTo_of_no_execOccurrence",
  "declaration:Exec.noRetainedWriteTo_of_sourceSites_no_exec",
  "declaration:Exec.noRetainedWriteTo_of_frame_owners_ne",
  "module:Blanc/ReachableExecFree.lean",
  "declaration:Prog.reachableExecFree",
  "declaration:Prog.reachableExecFree_iff",
  "declaration:Exec.Deriv.SourceCursor.Toward.linearDispatchWith_selectedBody",
  "declaration:Exec.Deriv.SourceCursor.noExec_of_reachableExecFree",
  "declaration:Exec.noRetainedWriteTo_of_no_sameFrame_execAt",
  "declaration:Exec.noRetainedWriteTo_of_exactMain_reachableExecFree",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "exact-retained-storage-effects"
status = "active"
triggers = ["goal-shape:exact-retained-storage-effects"]
preferred_path = "For a committing selected compiled walk, build `Func.RunCompiledTo.StorageEffectPath` in source order. For a long construction that must thread the indexed run and annotation together, use `Func.StorageEffectRun` and its source-shaped constructors; `next_effectNeutral` preserves the tail list across an ordinary non-SSTORE step, while `of_noRawSstorePath` packages an existing empty selected-path certificate. Use `storage_effect_run` to walk a childless neutral prefix with the `func_run` state/gas/hint engine; it hands external instructions, SSTOREs, internal calls, and terminals back to the caller. To replace a designated successful STOP with an exact-effect continuation, certify the selected neutral walk with `RunCompiledTo.SuccessfulStopPrefix.of_execFree` and apply `SuccessfulStopPrefix.splice`. Convert an existing raw-SSTORE-free certificate with `StorageEffectPath.of_noRawSstorePath`; convert an exact empty annotation back to raw freedom with `StorageEffectPath.noRawSstorePath_of_nil` or `StorageEffectRun.noRawSstorePath`. Otherwise supply `Ninst.ChildlessRunCompiled` at each synchronously resolved external step. Finish with `Prog.exists_exec_retainedStorageEffectTriples`, or `_appended` when the compiled program is only the exact prefix of a larger creation-code image. When composing an `Exec` directly, use `Exec.retainedStorageEffectTriples_cont`, `Exec.retainedStorageEffectTriples_doneOk`, and `Exec.retainedStorageEffectTriples_halt`."
boundary = "The certificate is exact retained chronology, including successful no-op SSTOREs. It requires a committing successful result and explicit childlessness at external steps; it does not infer child-frame absence from same-frame source classification or final storage equality."
owner_module = "Blanc/ForwardStorageEffects.lean"
canonical_example = "Blanc/ForwardStorageEffects.lean:Prog.exists_exec_retainedStorageEffectTriples"
symbols = [
  "module:Blanc/ForwardStorageEffects.lean",
  "declaration:Blanc.Ninst.storageEffectTriple?",
  "declaration:Blanc.Func.RunCompiledTo.StorageEffectPath",
  "declaration:Blanc.Func.RunCompiledTo.StorageEffectPath.next_of_not_exec",
  "declaration:Blanc.Func.RunCompiledTo.StorageEffectPath.of_noRawSstorePath",
  "declaration:Blanc.Func.RunCompiledTo.StorageEffectPath.noRawSstorePath_of_nil",
  "declaration:Blanc.Func.StorageEffectRun",
  "declaration:Blanc.Func.StorageEffectRun.of_noRawSstorePath",
  "declaration:Blanc.Func.StorageEffectRun.noRawSstorePath",
  "declaration:Blanc.Func.StorageEffectRun.last",
  "declaration:Blanc.Func.StorageEffectRun.next",
  "declaration:Blanc.Func.StorageEffectRun.next_effectNeutral",
  "declaration:Blanc.Func.StorageEffectRun.zero",
  "declaration:Blanc.Func.StorageEffectRun.succ",
  "declaration:Blanc.Func.StorageEffectRun.call",
  "declaration:Blanc.Func.storageEffectRun_branch_zero",
  "declaration:Blanc.Func.storageEffectRun_branch_succ",
  "declaration:Blanc.Func.SuccessStopOnly",
  "declaration:Blanc.Func.RunCompiledTo.SuccessfulStopPrefix",
  "declaration:Blanc.Func.RunCompiledTo.SuccessfulStopPrefix.of_execFree",
  "declaration:Blanc.Func.RunCompiledTo.SuccessfulStopPrefix.splice",
  "declaration:Blanc.Prog.exists_exec_retainedStorageEffectTriples",
  "declaration:Blanc.Prog.exists_exec_retainedStorageEffectTriples_appended",
  "declaration:Blanc.Exec.retainedStorageEffectTriples_cont",
  "declaration:Blanc.Exec.retainedStorageEffectTriples_doneOk",
  "declaration:Blanc.Exec.retainedStorageEffectTriples_halt",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "binary-dispatch-miss"
status = "active"
triggers = ["goal-shape:raw-sstore-free-compiled-path"]
preferred_path = "For an unmatched selector in a `DispatchTree`, prove `¬ tree.HasSelector selector` and apply `DispatchTree.dispatchMiss_runCompiledTo_with_path`. The result is the exact selector-dependent empty-revert `RunCompiledTo` paired with `NoRawSstorePath` for that identical proof; `DispatchTree.dispatchMissGas` names its exact cost."
boundary = "This follows only the selected miss path. It does not inspect or certify unselected sibling bodies, prove a contract-specific selector census, or add the selector-extraction and program-main prefix around `dispatch tree`."
owner_module = "Blanc/ForwardDispatchMiss.lean"
canonical_example = "Blanc/ForwardDispatchMiss.lean:DispatchTree.dispatchMiss_runCompiledTo_with_path"
symbols = [
  "module:Blanc/ForwardDispatchMiss.lean",
  "declaration:Blanc.DispatchTree.HasSelector",
  "declaration:Blanc.DispatchTree.dispatchMissGas",
  "declaration:Blanc.DispatchTree.dispatchMiss_runCompiledTo_with_path",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-31"

[[recipe]]
id = "accepted-boolean-settlement"
status = "active"
triggers = ["goal-shape:accepted-bool-word"]
preferred_path = "For a clean full-word ABI output, use `acceptedBoolWord_iff_of_output` instead of repeating the slice/read normalization. Remove a successful execution wrapper with `acceptedBoolExecution_ok_iff`, and specialize rejected-answer executions with `boolQueryExecutionFailure_ok_iff`."
boundary = "These adapters identify one already-clean 32-byte boolean observation. They do not prove message settlement, output production, or that the returned word is canonical zero or one."
owner_module = "Blanc/PinnedPauseTarget.lean"
canonical_example = "Blanc/PinnedPauseTarget.lean:acceptedBoolWord_iff_of_output"
symbols = [
  "module:Blanc/PinnedPauseTarget.lean",
  "declaration:acceptedBoolWord_iff_of_output",
  "declaration:acceptedBoolExecution_ok_iff",
  "declaration:boolQueryExecutionFailure_ok_iff",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "devm-common-update-laws"
status = "active"
triggers = ["goal-shape:devm-common-update-law"]
preferred_path = "Before proving a record projection by `rfl`, search the public `Devm` laws in `CommonProofs`: memory writes, accessed-storage/setMach cancellation, storage/code read-after-write, code-installation observations, and the reusable RETURN/SSTORE post projection cuts are named there. For Solidity address-typed storage, use `addressSlotReadWord`/`addressSlotWriteWord` and the value-carrying `loadAddressWordAt`/`storeAddressWordAt` inversions rather than restating mask algebra locally. Jaune also supplies update-first projection laws such as `Devm.memWrite_gasLeft` and `Devm.setMach_accessedStorageKeys`."
boundary = "Use the smallest abstract-base law that matches the goal. Do not unfold a concrete effect tower merely because these laws themselves are definitionally simple."
owner_module = "Blanc/CommonProofs.lean"
canonical_example = "Blanc/CommonProofs.lean:Devm.addAccessedStorageKey_setMach_setMach"
symbols = [
  "module:Blanc/CommonProofs.lean",
  "declaration:Devm.memWrite_memory",
  "declaration:Devm.memWrite_stack",
  "declaration:Devm.addAccessedStorageKey_setMach_setMach",
  "declaration:Devm.getStorVal_setStorVal_self",
  "declaration:Devm.setStorVal_getCode",
  "declaration:Devm.setCode_logs",
  "declaration:Devm.setCode_output",
  "declaration:Devm.setCode_error",
  "declaration:Devm.returnPost_getStorVal",
  "declaration:Devm.returnPost_accessedStorageKeys",
  "declaration:Devm.sstoreBase_state",
  "declaration:Devm.sstoreBase_accessedStorageKeys",
  "declaration:Devm.sstoreWarmBase_accessedStorageKeys",
  "module:Blanc/AddressSlot.lean",
  "module:Blanc/AddressSlotProofs.lean",
  "declaration:addressSlotReadWord",
  "declaration:addressSlotWriteWord",
  "declaration:addressSlotReadWord_eq_toAdr_toB256",
  "declaration:of_loadAddressWordAt_val",
  "declaration:of_storeAddressWordAt_val",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "compiled-terminal-at-zero"
status = "active"
triggers = ["goal-shape:terminal-return-revert"]
preferred_path = "For an offset-zero 32-byte RETURN or empty REVERT, use `Func.runCompiledTo_return_word_at_zero` or `Func.runCompiledTo_revert_empty_at_zero`; use the more general `Func.runCompiledTo_return_word` and `Func.runCompiledTo_revert` only when the offset, size, stack tail, or payload differs."
boundary = "These are construction lemmas for two common terminal shapes, not inversion theorems and not a replacement for the general terminal APIs."
owner_module = "Blanc/ExecutionTerminal.lean"
canonical_example = "Blanc/ExecutionTerminal.lean:Func.runCompiledTo_return_word_at_zero"
symbols = [
  "module:Blanc/ExecutionTerminal.lean",
  "declaration:Func.runCompiledTo_return_word_at_zero",
  "declaration:Func.runCompiledTo_revert_empty_at_zero",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-29"

[[recipe]]
id = "full-length-slice"
status = "active"
triggers = ["goal-shape:full-length-slice"]
preferred_path = "When a padded `sliceD` begins at zero and its requested width is the source length, rewrite with `Bytes.sliceD_zero_length` instead of reproving the take/drop normalization locally."
boundary = "The theorem needs exact equality between source length and requested width. It does not characterize nonzero offsets or shorter/longer windows."
owner_module = "Blanc/CommonProofs.lean"
canonical_example = "Blanc/CommonProofs.lean:Bytes.sliceD_zero_length"
symbols = [
  "module:Blanc/CommonProofs.lean",
  "declaration:Bytes.sliceD_zero_length",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-29"

[[recipe]]
id = "retained-wrapper-trace"
status = "active"
triggers = ["goal-shape:retained-wrapper-trace"]
preferred_path = "Choose the carrier at the wrapper boundary you actually have, then use its matching `exists_*Trace` theorem to retain Jaune's deterministic recursive witness. Start with `RetainedXlot` for a filled execution slot; use `ProcessMessageTrace.result` or `ProcessCreateMessageTrace.result` to recover the exact deterministic core-wrapper equation. At a retained delegated-child boundary, use `DelegatecallSpawnDescriptor.certificate_of_runCompiled` to invert an existing successful compiled step into its arbitrary child outcome and resume equation, `DelegatecallSpawnDescriptor.runCompiled_of_certificate` to construct the exact compiled step from such a child plus its resume equation, or `.settled_of_runCompiled` to retain the settled child together with exact parent observations. Then use `DelegatedChildCertificate.process`, `.result`, or `.rollback_of_error` for the retained child. Use `MessageCallTrace`, `TransactionTrace`, `AppliedBodyTrace`, or the configured block/history carriers instead of reconstructing a trace from only the terminal state."
boundary = "These carriers remember execution and wrapper structure but assign no contract-specific meaning to effects. Use the `Execution*Effects` modules for `ContractSpec` transport, `ExecutionPath` for stable call-tree locations, and the `Execution*StateTrace` modules for ordered world-state replay."
owner_module = "Blanc/ExecutionTrace.lean"
canonical_example = "Blanc/ExecutionTrace.lean:ExecutionTrace.exists_messageCallTrace"
symbols = [
  "module:Blanc/ExecutionTrace.lean",
  "module:Blanc/ExecutionHistory.lean",
  "module:Blanc/DelegatecallEnvelope.lean",
  "declaration:ExecutionTrace.RetainedXlot",
  "declaration:ExecutionTrace.exists_retainedXlot_of_filled",
  "declaration:ExecutionTrace.ProcessMessageTrace",
  "declaration:ExecutionTrace.exists_processMessageTrace",
  "declaration:ExecutionTrace.ProcessMessageTrace.result",
  "declaration:ExecutionTrace.ProcessCreateMessageTrace",
  "declaration:ExecutionTrace.exists_processCreateMessageTrace",
  "declaration:ExecutionTrace.ProcessCreateMessageTrace.result",
  "declaration:ExecutionTrace.MessageCallTrace",
  "declaration:ExecutionTrace.exists_messageCallTrace",
  "declaration:DelegatedChildCertificate",
  "declaration:DelegatecallSpawnDescriptor.certificate_of_runCompiled",
  "declaration:DelegatecallSpawnDescriptor.runCompiled_of_certificate",
  "declaration:DelegatecallSettledBoundary",
  "declaration:DelegatecallSpawnDescriptor.settled_of_runCompiled",
  "declaration:DelegatedChildCertificate.process",
  "declaration:DelegatedChildCertificate.result",
  "declaration:DelegatedChildCertificate.rollback_of_error",
  "declaration:ExecutionTrace.TransactionTrace",
  "declaration:ExecutionTrace.exists_transactionTrace",
  "declaration:ExecutionTrace.ApplyTransactionsTrace",
  "declaration:ExecutionTrace.exists_applyTransactionsTrace",
  "declaration:ExecutionTrace.SystemMessageTrace",
  "declaration:ExecutionTrace.exists_systemMessageTrace",
  "declaration:ExecutionTrace.RequestsTrace",
  "declaration:ExecutionTrace.exists_requestsTrace",
  "declaration:ExecutionTrace.AppliedBodyTrace",
  "declaration:ExecutionTrace.exists_appliedBodyTrace",
  "declaration:ExecutionTrace.ConfiguredBlockTrace",
  "declaration:ExecutionTrace.exists_configuredBlockTrace_of_transition",
  "declaration:ExecutionTrace.ConfiguredHistoryTrace",
  "declaration:ExecutionTrace.exists_configuredHistoryTrace_of_reachUsing",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "trace-local-frame-admission"
status = "active"
triggers = ["goal-shape:trace-local-frame-admission"]
preferred_path = "State the contract's entry condition as `Exec.FrameAdmitted ca entry` on the exact retained execution. Prove the frame obligation as `ContractSpec.SoundAdmitted ca entry`, close it with `ContractSpec.preserves_inv_admitted`, and use the matching `FrameAdmitted` plus `stateInv_admitted`/`benvInv_admitted` theorem on `MessageCallTrace`, `TransactionTrace`, `AppliedBodyTrace`, or `ConfiguredHistoryTrace` to transport it through only the interpreter frames actually entered. If the proof also needs canonical interpreter ingress, obtain `Exec.FreshEntry` from the carrier's `freshFrameAdmitted` theorem and combine it with the independent admission through the carrier's `FrameAdmitted.and`."
boundary = "Admission is evidence about concrete target-frame roots, not an execution constructor, settlement filter, or global environment restriction. Fresh-entry transport contributes only empty stack and memory and cannot discharge independent routing or world-state premises. The consumer must derive those premises from the retained trace; rollback and direct envelope effects remain governed by the ordinary wrapper transports."
owner_module = "Blanc/ExecutionAdmission.lean"
canonical_example = "Blanc/ExecutionHistoryAdmission.lean:ExecutionTrace.ConfiguredHistoryTrace.stateInv_admitted"
symbols = [
  "module:Blanc/ExecutionFrames.lean",
  "module:Blanc/ExecutionFrameEntry.lean",
  "module:Blanc/ExecutionAdmission.lean",
  "module:Blanc/ContractAdmission.lean",
  "module:Blanc/ExecutionMessageAdmission.lean",
  "module:Blanc/ExecutionTransactionAdmission.lean",
  "module:Blanc/ExecutionBodyAdmission.lean",
  "module:Blanc/ExecutionHistoryAdmission.lean",
  "module:Blanc/ExecutionTraceFresh.lean",
  "declaration:Exec.FrameAdmitted",
  "declaration:Exec.FreshEntry",
  "declaration:Exec.FrameAdmitted.and",
  "declaration:ExecutionTrace.ConfiguredHistoryTrace.FrameAdmitted.and",
  "declaration:ExecutionTrace.ConfiguredHistoryTrace.freshFrameAdmitted",
  "declaration:lift_inv_admitted",
  "declaration:ContractSpec.SoundAdmitted",
  "declaration:ContractSpec.PreservesAdmitted",
  "declaration:ContractSpec.preserves_inv_admitted",
  "declaration:ExecutionTrace.MessageCallTrace.stateInv_admitted",
  "declaration:ExecutionTrace.TransactionTrace.benvInv_admitted",
  "declaration:ExecutionTrace.AppliedBodyTrace.stateInv_admitted",
  "declaration:ExecutionTrace.ConfiguredHistoryTrace.stateInv_admitted",
]
review_owner = "proof-infrastructure"
review_date = "2026-09-01"

[[recipe]]
id = "retained-state-replay"
status = "active"
triggers = ["goal-head:StateReplay"]
preferred_path = "Build the chronology at the narrowest retained layer and finish with its `stateReplay` theorem. Use `Exec.committedStateReplay` for a recursive execution, then the message, transaction, body, block, or history chronology module to retain wrapper boundaries in their exact execution order. Compose or relabel an existing replay with `StateReplay.append`, `StateReplay.mapOrigin`, and `StateTransition.mapOrigin`."
boundary = "A `StateReplay` proves endpoint continuity and preserves exact provenance; it does not classify a transition as a contract deposit, withdrawal, or attack step. Apply that interpretation only in the contract-owned layer above the generic chronology."
owner_module = "Blanc/ExecutionStateTrace.lean"
canonical_example = "Blanc/ExecutionStateTrace.lean:Exec.committedStateReplay"
symbols = [
  "module:Blanc/ExecutionStateTrace.lean",
  "module:Blanc/ExecutionMessageStateTrace.lean",
  "module:Blanc/ExecutionTransactionStateTrace.lean",
  "module:Blanc/ExecutionBodyStateTrace.lean",
  "module:Blanc/ExecutionHistoryStateTrace.lean",
  "declaration:StateTransition",
  "declaration:StateReplay",
  "declaration:StateReplay.append",
  "declaration:StateTransition.mapOrigin",
  "declaration:StateReplay.mapOrigin",
  "declaration:Exec.committedStateReplay",
  "declaration:ExecutionTrace.MessageCallTrace.stateReplay",
  "declaration:ExecutionTrace.TransactionStateChronology.stateReplay",
  "declaration:ExecutionTrace.AppliedBodyStateChronology.stateReplay",
  "declaration:ExecutionTrace.ConfiguredHistoryStateChronology.stateReplay",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "constant-error-guard"
status = "active"
triggers = ["goal-shape:constant-error-guard"]
preferred_path = "Use `Func.runCompiledTo_errorGuard` when a nonzero branch flag tail-calls an auxiliary equal to `Func.revertWith reason`. Supply the auxiliary lookup, exact entry-state memory image and alignment, payload bounds, gas expressed through `errorGuardCost`, and stack room; the theorem returns the complete ABI `Error(string)` payload with exact final memory, stack, and gas. When an existential carrier exposes a different state with the same memory size, transport that exact cost with `errorGuardCost_congr_memory_size`."
boundary = "This proves the branch-and-internal-call walk only. It does not select a contract route, establish which flag is nonzero, authenticate a contract-specific reason/slot table, or turn the local revert into a public endpoint theorem. Keep `errorGuardCost` indexed by the actual entry state, or transport it only across a proved memory-size equality, so memory expansion is not silently weakened."
owner_module = "Blanc/RevertPayload.lean"
canonical_example = "Blanc/RevertPayload.lean:Func.runCompiledTo_errorGuard"
symbols = [
  "module:Blanc/RevertPayload.lean",
  "declaration:Blanc.errorBodyCost",
  "declaration:Blanc.errorCallCost",
  "declaration:Blanc.errorGuardCost",
  "declaration:Blanc.errorGuardCost_congr_memory_size",
  "declaration:Blanc.Func.runCompiledTo_revertWith",
  "declaration:Blanc.Func.runCompiledTo_errorGuard",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "one-word-source-return"
status = "active"
triggers = ["goal-head:ReturnsWord"]
preferred_path = "For the source fragment `mstoreAt 0 +++ returnMemoryRange 0 32`, use `of_storeReturnWord` when a `Mem.Wf`/`Mem.Reads` image is already available, or `returnsWord_of_storeReturn` when no memory side condition is in context. Both prove `ReturnsWord` from the known stack head and preserve code."
boundary = "This is the source-level one-word ABI observation. For a compiled terminal walk use `Func.runCompiledTo_return_word_at_zero`; for other offsets, sizes, or payloads use the general return APIs."
owner_module = "Blanc/Ladder.lean"
canonical_example = "Blanc/Ladder.lean:returnsWord_of_storeReturn"
symbols = [
  "module:Blanc/Ladder.lean",
  "declaration:ReturnsWord",
  "declaration:of_storeReturnWord",
  "declaration:returnsWord_of_storeReturn",
]
review_owner = "proof-infrastructure"
review_date = "2026-08-30"

[[recipe]]
id = "upgrade-migration-refinement"
status = "active"
triggers = ["goal-head:MigrationSound", "goal-head:BehavioralRefinement"]
preferred_path = "Start from `UpgradeArchitecture`, keeping `proxyProg`, v1, v2, the migration, and the relation explicit. Prove `MigrationSound` from the named state transformer independently of `BehavioralRefinement`; then prove the latter from version-specific shared steps. A product theorem must separately connect an exact proxy execution to the migration."
boundary = "These predicates describe a migration and shared logical behavior. They do not execute a proxy, establish authorization or installation, transport a child through delegatecall, prove rollback, or turn one predicate into evidence for the other."
owner_module = "Blanc/Upgrade.lean"
canonical_example = "Blanc/Upgrade.lean:MigrationSound"
symbols = [
  "module:Blanc/Upgrade.lean",
  "declaration:Blanc.UpgradeArchitecture",
  "declaration:Blanc.MigrationSound",
  "declaration:Blanc.BehavioralRefinement",
]
review_owner = "proof-infrastructure"
review_date = "2026-09-01"
