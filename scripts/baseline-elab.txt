# Elaboration-time baseline for Blanc — scripts/check-elab.sh
#
# STATUS<TAB>TIME<TAB>path. TIME is seconds to re-elaborate that file against
# already-built dependencies, measured sequentially with no language server
# alive. A file fails the gate above both 2.0x its time here and that time
# plus 1.0s. Rewrite with: scripts/check-elab.sh --rebase
#
# The Blanc/Conserved.lean row was hand-raised from 1.235 to its measured
# 2.178 when Arc B of ~/plans/flashmint-proposal.md turned that module from a
# 123-line statement stub into fmint's conservation proof: growth there was
# the deliverable, not drift. Every other row is from the last full rebase,
# and the gate was green against the old row when the edit was made.
#
# The Blanc/Compiled.lean row was hand-set to its measured 1.207 when Step 1
# of ~/plans/liveness-prelude.md added that module: a file with no row here is
# a REGRESSION ("a new module must state its cost"), so the row is an
# extension to admit new content, not a relaxation to admit a failure. It was
# measured by `scripts/check-elab.sh --list --no-build` on a tree with no
# language server alive, and it is re-set by hand at each later step of that
# arc that grows the module. `--rebase` was not run: it rewrites every row and
# would launder unrelated drift into an arc commit.
#
# Step 2 of that arc (the forward direction, `runCompiled_of_exec`) re-set the
# row from 1.207 to its measured 1.309 while nearly tripling the module's
# length. The proof reuses `correct_core`'s structure rather than restating it,
# so nearly all the added text is a thin gas-accounting layer; the one
# expensive-looking declaration, `Rinst.runCore_pc_irrel`, is a ~70-constructor
# case bash whose every case closes by `rfl`. Same measurement conditions, and
# again by hand rather than `--rebase`.
#
# Step 3 of that arc (jumpability of compiled jump targets) re-set the row from
# 1.309 to its measured 1.608. The added material is all at the level of
# Jaune's bytes: a specification for `noPushBefore`'s backward scan, the
# transport lemma over one complete instruction, and the `Func.compile`- and
# `Table.compile`-shaped walks that carry it. No opcode inductive is
# case-bashed -- the one fact needed about opcode bytes goes through the
# existing `toInstType` lemmas -- so the growth is proportional to the text.
# Same measurement conditions, and again by hand rather than `--rebase`.
#
# Step 4 of that arc (the liveness direction and the biconditional) re-set the
# row from 1.608 to its measured 1.786. The added material constructs `Exec`
# derivations from `RunCompiled` premises: forward evaluation of
# `chargeGas`/`push`/`pop`, one `.cont` equation per hidden instruction, a
# 14-projection extensionality bridge, and the derivation induction. Every
# state identification is by projection `rfl`s plus one `omega`, so the growth
# is again proportional to the text. Same measurement conditions, and again by
# hand rather than `--rebase`.
#
# 2026-08-06. Step 3 of ~/plans/fmint-restoration.md re-set the
# Blanc/FlashSpec.lean row from 1.146 to its measured 2.002. Be clear about
# what that raise is made of, because only part of it is this arc's:
#
#   * ~0.12 s IS this arc's growth. Step 2 added `rollback_of_callback_failure`
#     (1.844 -> 1.884) and Step 3 added the restoration family -- the abstract
#     core `rollback_of_no_success`, its total-function corollary, and seven
#     one-line instantiations, one per landed `no_success_of_*` (1.884 ->
#     ~2.00). The core is ~25 tactic lines; the other nine statements are term
#     proofs one application deep, so the growth is proportional to the text
#     and not to any new proof search.
#   * ~0.73 s was ALREADY there and is not this arc's. The 1.146 row has been
#     below the module's measured cost since Arc C of
#     ~/plans/flashmint-proposal.md, which reported 1.931 against it; the
#     planning session for this arc measured 1.874 on untouched `main`. This
#     arc did not create that gap and is not hiding it: the closure report
#     (~/plans/reports/fmint-restoration-step-3.md) states both numbers.
#
# The raise is taken from GREEN, not to rescue a red gate: at the old row the
# module measured 1.982-2.134 against a 2.292 threshold and passed every run.
# It is taken anyway because ~0.16 s of margin is inside this host's observed
# noise -- during this step's verification `Blanc.lean` (untouched by the arc)
# spiked from its 1.241 row to 3.651 on one contended run and returned to
# 1.249 on the next -- and the next arc to grow this module should meet a row
# that reflects the module, not one that would force a re-set from red.
#
# 2.002 is the median of three sequential measurements of the merge candidate
# (2.002, 2.134, 1.982), each taken with no language server alive.
# `--rebase` was NOT run: it rewrites every row and would launder unrelated
# drift into an arc commit. Blanc/CommonProofs.lean (6.143, measured ~6.97)
# and Blanc/Ladder.lean (3.121, measured ~3.55) are stale in the same
# direction and were deliberately LEFT ALONE -- their thresholds are 12.286
# and 6.242, so neither is close, and re-setting a row that is not at risk
# would be drift laundering with no benefit.
#
# The Blanc/Forward.lean row was hand-set to 1.188 when Step 1 of
# ~/plans/forward-witness.md added that module, on the same rule and for the
# same reason as Blanc/Compiled.lean's above: a file with no row here is a
# REGRESSION, so a new module's row is an extension to admit new content, not
# a relaxation to admit a failure. 1.188 is the median of three sequential
# `scripts/check-elab.sh --list --no-build` measurements (1.181, 1.188, 1.193)
# with no language server alive. `--rebase` and `--force` are prohibited for
# the whole of that arc, for the reason already recorded above.
OK	1.241	Blanc.lean
OK	1.257	Blanc/Basic.lean
OK	2.454	Blanc/CommonCore.lean
OK	6.143	Blanc/CommonProofs.lean
OK	1.786	Blanc/Compiled.lean
OK	2.178	Blanc/Conserved.lean
OK	3.158	Blanc/Fmint.lean
OK	4.549	Blanc/FmintCode.lean
OK	3.121	Blanc/Ladder.lean
OK	1.656	Blanc/Semantics.lean
OK	2.053	Blanc/Solvent.lean
OK	2.113	Blanc/Tactics.lean
OK	2.461	Blanc/Weth.lean
OK	3.845	Blanc/WethCode.lean
OK	1.283	Main.lean
OK	2.002	Blanc/FlashSpec.lean
OK	1.188	Blanc/Forward.lean
