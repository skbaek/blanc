# scripts/fmint-coverage-budget.txt
#
# The known-unreached subset of fmint's twelve selectors
# (scripts/fmint-selectors.json, emitted from Blanc.Fmint.fmintFuncs by
# scripts/gen-fmint-selectors.lean), checked by
# scripts/check-fmint-coverage.sh (~/plans/fmint-code.md Step 2, mirroring
# ~/plans/weth-evidence.md Step 2). This is a budget in the idiom of
# Jaune's scripts/check-integrity.sh pending-budget: the gate fails if the
# actual set of unreached selectors is not a subset of the rows below, or if
# that set's size exceeds the declared budget number. A row lists a selector
# this suite is KNOWN not to reach yet, with a trailing comment
# naming the function -- the comment is documentation only, never compared.
#
# The gate credits two direct top-level entries and seven exact internal CALL
# sites whose straight-line prop records a changed flag/marker after the CALL.
# Three selectors occur only as PUSH4 literals in branching borrower code.
# Presence is not execution evidence, so those three are honestly budgeted.
# This 0 -> 3 correction is the one-time repair of the former embedding-based
# gate, not ordinary budget growth. Future changes may only shrink it.
#
# The budget may only SHRINK. Removing a row -- because a later step added a
# case that reaches it -- is the intended way this file changes; adding a
# row is a regression and is not a routine step.
#
# coverage-budget: 3
0x18160ddd  ## totalSupply() -- borrower embedding has no callsite witness
0x70a08231  ## balanceOf(address) -- borrower embedding has no callsite witness
0xa9059cbb  ## transfer(address,uint256) -- borrower embedding has no durable witness
