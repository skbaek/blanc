# Exact allowlist for scripts/check-trust-surface.sh.
#
# Data rows have the form
#   <rule> <relative path> <normalized complete source line> ## justification
#
# The gate is fail-closed in both directions: a new occurrence and a stale row
# both fail.  Comment/docstring mentions are listed rather than lexically
# discarded, so changing their wording forces review too.

# Comment-only descriptions of what the proof does not assume.
S2-axiom Blanc/Weth10Permit.lean This is not a cryptographic correctness axiom. It is the exact result of ## comment-only non-claim
S2-axiom Blanc/Weth10Permit.lean supplied to KECCAK256, are identified without a hash axiom. -/ ## comment-only non-claim

# Comment/docstring uses of "opaque" in its ordinary proof-engineering sense.
S3-opaque Blanc/CommonCore.lean -- resulting tree. Keeping the comparisons in `sorted` leaves the leaves opaque. ## comment only
S3-opaque Blanc/CommonProofs.lean on why the leaves must stay opaque). -/ ## comment only
S3-opaque Blanc/FlashSpec.lean `fmintFuncs_sorted`'s docstring). Proofs treat it as an opaque word. -/ ## comment only
S3-opaque Blanc/ForwardCall.lean derivation is `exec cevm`, an opaque term the caller case-splits on. -/ ## comment only
S3-opaque Blanc/Weth10DeployDomainSlices.lean off-path subtrees opaque. -/ ## comment only

# Compile-witness docstrings explicitly distinguish kernel evaluation from the
# forbidden native evaluator.
S6-native-decide Blanc/FmintCode.lean *not* `native_decide`). -/ ## comment-only trust explanation
S6-native-decide Blanc/LidoCircuitBreakerPinnedTargetControl.lean native_decide ## closed compiled-stub byte facts and executable semantic controls
S6-native-decide Blanc/LidoCircuitBreakerPinnedTargetComposition.lean native_decide ## closed compiled-stub byte facts used by the occurrence bridge
S6-native-decide Blanc/LidoCircuitBreakerPinnedTargetCompositionControl.lean native_decide ## closed total-exec production-world control
S6-native-decide Blanc/WethCode.lean trusted base (in particular this is *not* `native_decide`). -/ ## comment-only trust explanation

# Terminating-by-construction metaprograms and tactics. These declarations
# operate on Lean expressions/goals; no object-level Blanc function is partial.
S7-partial-def Blanc/Forward.lean partial def funcWalk (g : MVarId) : ForwardM Unit := g.withContext do ## Meta traversal
S7-partial-def Blanc/Forward.lean partial def whnfUntilHead (heads : List Name) : Nat → Expr → MetaM Expr ## Meta normalization
S7-partial-def Blanc/Tactics.lean partial def funcInv : Lean.Elab.Tactic.TacticM Unit := ## tactic elaborator
S7-partial-def Blanc/Tactics.lean partial def lineInv : Lean.Elab.Tactic.TacticM Unit := ## tactic elaborator
S7-partial-def Blanc/Tactics.lean partial def linePrefix : Lean.Elab.Tactic.TacticM Unit := ## tactic elaborator

# Diagnostics emitted only while the custom tactics themselves fail or report
# unsupported proof shapes; they are not executable Blanc/EVM behavior.
S8-dbg-trace Blanc/Tactics.lean dbg_trace "Not a pref goal : " ## tactic diagnostic
S8-dbg-trace Blanc/Tactics.lean dbg_trace s; failure ## tactic diagnostic
S8-dbg-trace Blanc/Tactics.lean dbg_trace t ## tactic diagnostic
S8-dbg-trace Blanc/Tactics.lean | _ => dbg_trace "Not a Line.Inv goal" ## tactic diagnostic
S8-dbg-trace Blanc/Tactics.lean | _ => dbg_trace "Not a Linst.Inv goal" ## tactic diagnostic
S8-dbg-trace Blanc/Tactics.lean | _ => dbg_trace "Not a Ninst.Inv goal" ## tactic diagnostic
S8-dbg-trace Blanc/Tactics.lean | _ => dbg_trace "line_prefix : unimplemented inst"; failure ## tactic diagnostic
