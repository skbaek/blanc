import Blanc.Forward

/-!
# Compatibility import for `MSTORE8` forward rules

The contract-neutral construction rules and `func_run` arm now live in
`Blanc.Forward`. This module remains as a compatibility import for existing
consumers.
-/
